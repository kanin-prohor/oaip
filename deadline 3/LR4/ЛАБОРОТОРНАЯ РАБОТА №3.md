# №1
```mermaid
classDiagram
    class SmartLight {
        +int brightness
        +string color
        -bool __is_on
        
        +__init__(brightness: int = 50, color: str = "white")
        +turn_on() void
        +turn_off() void
        +set_color(new_color: str) void
        +get_status() str
    }
```
# №2
```mermaid
classDiagram
    class CreditCard {
        +string owner_name
        -string __card_number
        -string __cvc
        -string __pin
        #float _balance
        
        +__init__(owner_name: str, card_number: str, cvc: str, pin: str, initial_balance: float)
        +pay(amount: float, pin: str) bool
        -__check_pin(entered_pin: str) bool
        +get_masked_card_number() str
        +get_balance() float
        +deposit(amount: float) void
    }

```
# №3
```mermaid
classDiagram
    class Character {
        +string name
        +__init__(name: str)
        +move() void
        +speak() void
    }
    
    class Archer {
        +int arrow_count
        +__init__(name: str, arrow_count: int)
        +shoot() void
        +reload(arrows: int) void
    }
    
    class Knight {
        +int sword_damage
        +__init__(name: str, sword_damage: int)
        +attack_sword() void
        +defend() void
    }
    
    class Mage {
        +int mana
        +__init__(name: str, mana: int)
        +cast_spell(spell_name: str, mana_cost: int) void
    }
    
    Character <|-- Archer
    Character <|-- Knight
    Character <|-- Mage
```
# №4
```mermaid
classDiagram
    class Song {
        +string title
        +float duration
        +__init__(title: str, duration: float)
        +get_duration_str() str
        +__str__() str
    }
    
    class Playlist {
        +string name
        +list songs
        +__init__(name: str)
        +add_song(song: Song) void
        +remove_song(song_title: str) bool
        +get_total_duration() float
        +get_total_duration_str() str
        +get_songs_count() int
        +show_playlist() void
        +search_song(keyword: str) list
    }
    
    Playlist "1" -- "*" Song : содержит
```
# №5
```mermaid
classDiagram
    class Product {
        +string name
        +float price
        +__init__(name: str, price: float)
        +__str__() str
    }
    
    class Order {
        +string order_id
        +string client_name
        +string address
        +string status
        +list products
        +DeliveryAgent delivery_agent
        +__init__(order_id: str, client_name: str, address: str)
        +add_product(product: Product) void
        +get_total_price() float
        +change_status(status: str) void
        +assign_delivery_agent(agent: DeliveryAgent) void
        +process_delivery() void
        +__str__() str
    }
    
    class DeliveryAgent {
        <<abstract>>
        +string agent_id
        +float speed
        +Order current_order
        +__init__(agent_id: str, speed: float)
        +deliver(address: str)* void
        +assign_order(order: Order) void
    }
    
    class Courier {
        +string phone
        +__init__(agent_id: str, speed: float, phone: str)
        +deliver(address: str) void
        +call_client(client_phone: str) void
    }
    
    class Drone {
        +float max_altitude
        +bool is_flying
        +__init__(agent_id: str, speed: float, max_altitude: float)
        +deliver(address: str) void
        +take_off() void
        +fly() void
        +land() void
    }
    
    Order "*" -- "1" DeliveryAgent : закреплен за
    Order "1" *-- "*" Product : состоит из
    DeliveryAgent <|-- Courier
    DeliveryAgent <|-- Drone
```
# №6
```mermaid
classDiagram
    class User {
        +string user_id
        +string username
        +string email
        +list posts
        +datetime created_at
        +__init__(user_id: str, username: str, email: str)
        +create_post(content: str) Post
        +delete_post(post: Post) bool
        +get_posts_count() int
        +__str__() str
    }
    
    class Post {
        +string content
        +User author
        +list comments
        +datetime created_at
        +int likes
        +__init__(content: str, author: User)
        +add_comment(user: User, text: str) Comment
        +add_like() void
        +get_comments_count() int
        +__str__() str
    }
    
    class Comment {
        +string text
        +User author
        +Post post
        +datetime created_at
        +__init__(text: str, author: User, post: Post)
        +__str__() str
    }
    
    class SocialNetwork {
        +string name
        +dict users
        +list posts
        +__init__(name: str)
        +register_user(username: str, email: str) User
        +create_post(user_id: str, content: str) Post
        +get_timeline(limit: int) list
        +__str__() str
    }
    
    User "1" o-- "*" Post : создает
    Post "*" -- "1" User : принадлежит
    Post "1" *-- "*" Comment : содержит
    Comment "*" -- "1" Post : относится к
    Comment "*" -- "1" User : автор
    SocialNetwork "1" o-- "*" User : управляет
    SocialNetwork "1" o-- "*" Post : хранит
```
**Обоснование выбора связей:**

1. **Пользователь → Пост (`o--` - Агрегация)**:
    
    - Пользователь может создавать много постов, но пост может существовать и без пользователя (если пользователь удален, посты могут остаться в архиве).
        
    - В реальной социальной сети посты часто остаются даже после удаления аккаунта.
        
    - **Агрегация**, а не композиция, потому что пост имеет самостоятельную ценность и логику.
        
2. **Пост → Комментарий (`*--` - Композиция)**:
    
    - Комментарии не имеют смысла без поста. Если пост удаляется, все комментарии должны удаляться автоматически.
        
    - Комментарии - это часть контента поста, они тесно связаны с ним.
        
    - **Композиция**, потому что комментарии являются неотъемлемой частью поста.
        
3. **Комментарий → Пользователь (`--` - Ассоциация)**:
    
    - Комментарий ссылается на пользователя, который его оставил, но пользователь может существовать без комментариев.
        
    - Простая ассоциация, так как пользователь и комментарий относительно независимы.